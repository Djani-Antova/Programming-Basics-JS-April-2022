function sequence2kPlus1(input) {
    let num = Number(input[0]);
    let counter = 1;

    while (counter <= num) {
        console.log(counter);
        counter = counter * 2 + 1;
        // nextNum = prevNum * 2 +1
    }
}
sequence2kPlus1 (["31"])